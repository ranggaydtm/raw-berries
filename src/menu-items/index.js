/* eslint-disable no-unused-vars */
import dashboard from './dashboard';
import onProgress from './progress';
import sidebar from './sidebar';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    // items: [dashboard, onProgress]
    items: [sidebar]
};

export default menuItems;
